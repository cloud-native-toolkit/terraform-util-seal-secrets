name: seal-secrets-util
type: terraform
description: Module to encrypt or "seal" kube secrets using kubeseal cli and provided cert. Each of the yaml files in the `source_dir` is
  encrypted with the cert and written into the `dest_dir` directory.
tags:
    - sealed secrets
    - util
versions:
- platforms: []
  dependencies: []
  variables: []
